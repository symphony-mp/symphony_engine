/// Track metadata entity.
final class TrackMetaEntity {
  /// Creates a new [TrackMetaEntity].
  const TrackMetaEntity({
    this.total,
    this.number,
  });

  /// Track total.
  final int? total;

  /// Track number.
  final int? number;
}
